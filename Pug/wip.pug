extends ./index.pug

mixin css(src)
	-src = 'CSS/' + src
	link(href=src, rel="stylesheet", type="text/css")
mixin script(src)
	-src = 'JS/' + src
	script(type="text/javascript", src=src)

block title
	| WIP Project

block head
	+css('Projects.css')
	+script('Projects.js')

block content
	#projectWrapper
		.project(srcLink='#', playLink='#', projectName='Technomancer')
		.project(srcLink='#', playLink='#', projectName='Cartmaster')
		.project(srcLink='#', playLink='#',projectName='Questmaster')
		.project(srcLink='#', playLink='#', projectName='Chunk Escape')